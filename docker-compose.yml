services:
  mongodb:
    image: mongo:7
    container_name: voyara-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: voyara
    volumes:
      - voyara_mongodb_data:/data/db

volumes:
  voyara_mongodb_data:
