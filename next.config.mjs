/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      // Add any image hosts you need here
      // Example: { protocol: 'https', hostname: 'example.com' }
    ],
  },
};

export default nextConfig;